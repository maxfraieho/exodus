name: Generate Repository XML and Upload to MinIO via Cloudflare

on:
  push:
    branches: [main]
    paths:
      - "src/site/notes/**"
      - "*.*"
  pull_request:
    branches: [main]
    paths:
      - "src/site/notes/**"
      - "*.*"

jobs:
  generate-and-upload:
    runs-on: ubuntu-latest

    env:
      MINIO_ENDPOINT_URL: https://apiminio.exodus.pp.ua
      AWS_ACCESS_KEY_ID: ${{ secrets.MINIO_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.MINIO_SECRET_KEY }}
      AWS_DEFAULT_REGION: us-east-1

    steps:
      # Крок 1: Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # Крок 2: Встановлення залежностей і AWS CLI (оновлено)
      - name: Install system dependencies and AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip curl bc git # Додали bc для обчислень, git для перевірки існування директорій
          curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
          unzip -q awscliv2.zip
          sudo ./aws/install --update
          aws --version

      # Крок 3: Налаштування AWS CLI для MinIO через Cloudflare
      - name: Configure AWS CLI for MinIO via Cloudflare
        run: |
          aws configure set default.region us-east-1
          aws configure set default.s3.addressing_style path
          aws configure set default.s3.signature_version s3v4
          aws configure set default.s3.max_concurrent_requests 1
          aws configure set default.s3.max_bandwidth 50MB/s

      # Крок 4: Перевірка доступу до MinIO
      - name: Verify AWS CLI credentials with MinIO
        run: |
          echo "🔐 Перевірка доступу до MinIO..."
          if ! aws --endpoint-url "$MINIO_ENDPOINT_URL" s3 ls; then
            echo "❌ Помилка авторизації або недоступний MinIO endpoint"
            exit 1
          fi
          echo "✅ Доступ до MinIO підтверджено"

      # Крок 5: Генерація transliteration скрипта
      - name: Generate transliteration script
        run: |
          cat > transliterate.sh << 'EOF'
          #!/bin/bash
          transliterate() {
              local input="$1"
              # Транслітерація українських літер
              input=$(echo "$input" | sed 's/а/a/g; s/б/b/g; s/в/v/g; s/г/h/g; s/ґ/g/g; s/д/d/g; s/е/e/g; s/є/ye/g; s/ж/zh/g; s/з/z/g; s/и/y/g; s/і/i/g; s/ї/yi/g; s/й/y/g; s/к/k/g; s/л/l/g; s/м/m/g; s/н/n/g; s/о/o/g; s/п/p/g; s/р/r/g; s/с/s/g; s/т/t/g; s/у/u/g; s/ф/f/g; s/х/kh/g; s/ц/ts/g; s/ч/ch/g; s/ш/sh/g; s/щ/shch/g; s/ь//g; s/ю/yu/g; s/я/ya/g')
              input=$(echo "$input" | sed 's/А/A/g; s/Б/B/g; s/В/V/g; s/Г/H/g; s/Ґ/G/g; s/Д/D/g; s/Е/E/g; s/Є/Ye/g; s/Ж/Zh/g; s/З/Z/g; s/И/Y/g; s/І/I/g; s/Ї/Yi/g; s/Й/Y/g; s/К/K/g; s/Л/L/g; s/М/M/g; s/Н/N/g; s/О/O/g; s/П/P/g; s/Р/R/g; s/С/S/g; s/Т/T/g; s/У/U/g; s/Ф/F/g; s/Х/Kh/g; s/Ц/Ts/g; s/Ч/Ch/g; s/Ш/Sh/g; s/Щ/Shch/g; s/Ь//g; s/Ю/Yu/g; s/Я/Ya/g')
              # Заміна пробілів на дефіси та переведення у нижній регістр
              input=$(echo "$input" | sed 's/[[:space:]]\+/-/g' | tr '[:upper:]' '[:lower:]')
              # Видалення всіх символів, крім літер латиниці, цифр та дефісів
              input=$(echo "$input" | sed 's/[^a-z0-9-]//g' | sed 's/--*/-/g; s/^-//; s/-$//')
              
              # Обмеження довжини назви бакету згідно правил MinIO/S3
              # Мінімальна довжина 3 символи (додаємо "bucket", якщо коротше)
              if [ ${#input} -lt 3 ]; then 
                  input="${input}bucket"
              fi
              # Максимальна довжина 63 символи
              if [ ${#input} -gt 63 ]; then 
                  input="${input:0:63}"
              fi
              # Перевірка, що назва бакету відповідає шаблону: починається і закінчується літерою/цифрою, містить літери, цифри або дефіси
              if [[ ! "$input" =~ ^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$ ]]; then
                echo "⚠️ Попередження: Транслітерована назва бакету '$input' може бути некоректною. Спроба виправити..."
                input=$(echo "$input" | sed 's/[^a-z0-9-]//g') # Видаляємо будь-які недопустимі символи
                if [ ${#input} -lt 3 ]; then input="${input}bucket"; fi
                if [ ${#input} -gt 63 ]; then input="${input:0:63}"; fi
                # Фінальна перевірка
                if [[ ! "$input" =~ ^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$ ]]; then
                  echo "❌ Критична помилка: Не вдалося створити коректну назву бакету з '$1'. Остаточна назва: '$input'"
                  exit 1
                fi
              fi
              echo "$input"
          }
          transliterate "$1"
          EOF
          chmod +x transliterate.sh

      # Крок 6: Генерація XML для кожної папки
      - name: Generate XML files for each folder
        run: |
          mkdir -p xml-outputs
          if [ ! -d "src/site/notes/" ]; then
            echo "❌ Директорія src/site/notes/ не знайдена."
            # Якщо немає папок з нотатками, можливо, це нормально, якщо не хочемо створювати common.xml.
            # Але якщо очікується генерація файлів, то це помилка. Залишаємо exit 1.
            exit 1
          fi

          found_notes_folders=false
          for folder in src/site/notes/*/; do
            if [ -d "$folder" ]; then
              found_notes_folders=true
              folder_name=$(basename "$folder")
              bucket_name=$(./transliterate.sh "$folder_name")

              echo "🔄 Генерація XML для папки: $folder_name (Бакет: $bucket_name)"
              
              # Перевірка, чи repomix встановлено
              if ! command -v repomix &> /dev/null
              then
                  echo "❌ repomix не знайдено. Встановлення repomix..."
                  npm install -g repomix
                  if [ $? -ne 0 ]; then
                      echo "❌ Помилка встановлення repomix."
                      exit 1
                  fi
              fi

              # Виконання repomix
              npx repomix --config repomix.config.json --include "$folder/**" --output "xml-outputs/${bucket_name}.xml"
              
              if [ ! -s "xml-outputs/${bucket_name}.xml" ]; then
                echo "❌ XML-файл порожній або не створений для '$folder_name' (вивід: xml-outputs/${bucket_name}.xml)"
                exit 1
              fi

              echo "✅ Згенеровано: ${bucket_name}.xml"
            fi
          done
          
          # Якщо не було знайдено жодної папки в src/site/notes/, але common.xml має генеруватися, то це може бути помилкою.
          # Проте, якщо сценарій полягає в тому, щоб генерувати common.xml лише тоді, коли є інші папки, то це нормально.
          # Припустимо, що common.xml генерується завжди, якщо є файли поза src/site/notes/.

      # Крок 7: Генерація common.xml для кореневих файлів
      - name: Generate common.xml for root files
        run: |
          echo "🔄 Генерація common.xml для кореневих файлів..."
          
          # Перевірка, чи repomix встановлено
          if ! command -v repomix &> /dev/null
          then
              echo "❌ repomix не знайдено. Встановлення repomix..."
              npm install -g repomix
              if [ $? -ne 0 ]; then
                  echo "❌ Помилка встановлення repomix."
                  exit 1
              fi
          fi
          
          # Генерація common.xml з виключенням папки з нотатками
          npx repomix --config repomix.config.json --ignore "src/site/notes/**" --output "xml-outputs/common.xml"
          
          # Перевірка, чи common.xml був створений і не є порожнім
          if [ ! -s "xml-outputs/common.xml" ]; then
            echo "⚠️ common.xml порожній або не створений. Можливо, немає файлів поза src/site/notes/."
            # Замість exit 1, тут може бути логічно пропустити цей файл або просто проігнорувати помилку, якщо це не критично.
            # Для безпеки, якщо очікується завжди один common.xml, то залишаємо exit 1.
            # Якщо ж common.xml може бути відсутній, то замінити на: echo "⚠️ common.xml порожній або не створений."
            # exit 1 # Закоментовано, щоб продовжити, якщо файлів поза директорією немає.
            echo "✅ common.xml не створено або порожній (це може бути нормальним, якщо немає файлів поза src/site/notes/)."
          else
            echo "✅ Згенеровано: common.xml"
          fi

      # Крок 8: Upload XML outputs as artifacts
      - name: Upload XML outputs as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xml-outputs
          path: xml-outputs/
          retention-days: 7

      # Крок 9: Встановлення MinIO Client
      - name: Install MinIO Client
        run: |
          wget https://dl.min.io/client/mc/release/linux-amd64/mc
          chmod +x mc
          sudo mv mc /usr/local/bin/
          mc --version

      # Крок 10: Конфігурація MinIO Client
      - name: Configure MinIO Client
        run: |
          mc alias set minio "$MINIO_ENDPOINT_URL" "$AWS_ACCESS_KEY_ID" "$AWS_SECRET_ACCESS_KEY" --api S3v4

      # Крок 11: Завантаження XML-файлів у MinIO через mc
      - name: Upload XML files with MinIO Client
        run: |
          for xml_file in xml-outputs/*.xml; do
            if [ -f "$xml_file" ]; then # Перевіряємо, чи файл існує
              bucket_name=$(basename "$xml_file" .xml)
              echo "📤 Завантаження файлу: $xml_file -> minio/$bucket_name/"
              
              # Створення бакету (якщо не існує)
              mc mb -q --ignore-existing minio/$bucket_name
              if [ $? -ne 0 ]; then
                echo "❌ Помилка при створенні бакету '$bucket_name'."
                exit 1
              fi
              
              # Копіювання файлу
              mc cp "$xml_file" "minio/$bucket_name/${bucket_name}.xml"
              if [ $? -ne 0 ]; then
                echo "❌ Помилка при копіюванні файлу '$xml_file' у бакет '$bucket_name'."
                exit 1
              fi
              
              # Додаємо затримку після копіювання (не завжди потрібна, але може допомогти при проблемах з синхронізацією)
              # echo "⏳ Очікування синхронізації (5 секунд)..."
              # sleep 5 # Зазвичай краще покладатися на перевірку після завантаження
              
              echo "✅ Завантажено: ${bucket_name}.xml"
            fi
          done

      # Крок 12: Перевірка завантаження з конвертацією одиниць та допуском
      - name: Verify uploaded files with retry logic
        run: |
          # Функція для конвертації human-readable size в байти
          to_bytes() {
            local size_str="$1"
            local unit="$2"
            local size_bytes=0

            # Видаляємо пробіли з числа, якщо вони є (наприклад, 209 807)
            size_str=$(echo "$size_str" | tr -d ' ')

            case "$unit" in
              KiB) size_bytes=$(echo "$size_str * 1024" | bc -l) ;;
              MiB) size_bytes=$(echo "$size_str * 1024 * 1024" | bc -l) ;;
              GiB) size_bytes=$(echo "$size_str * 1024 * 1024 * 1024" | bc -l) ;;
              TiB) size_bytes=$(echo "$size_str * 1024 * 1024 * 1024 * 1024" | bc -l) ;;
              kB)  size_bytes=$(echo "$size_str * 1000" | bc -l) ;;
              MB)  size_bytes=$(echo "$size_str * 1000 * 1000" | bc -l) ;;
              GB)  size_bytes=$(echo "$size_str * 1000 * 1000 * 1000" | bc -l) ;;
              TB)  size_bytes=$(echo "$size_str * 1000 * 1000 * 1000 * 1000" | bc -l) ;;
              Bytes|Byte|B) size_bytes="$size_str" ;; # Якщо одиниця Bytes, просто беремо значення
              *)
                echo "⚠️ Невідома одиниця виміру: $unit"
                return 1 # Повертаємо помилку, якщо одиниця невідома
                ;;
            esac
            # Округлюємо до цілого числа байтів
            echo "$size_bytes" | awk '{printf "%d\n", $1}'
          }

          for xml_file in xml-outputs/*.xml; do
            if [ -f "$xml_file" ]; then # Перевіряємо, чи файл дійсно існує
              filename=$(basename "$xml_file")
              bucket_name=$(basename "$xml_file" .xml)
              local_size=$(wc -c < "$xml_file" | tr -d ' ')
              
              echo "🔍 Перевірка файлу: $filename"
              echo "📊 Локальний розмір: $local_size байт"
              
              remote_bytes=""
              # Спроби отримати розмір віддаленого файлу
              for attempt in {1..5}; do
                echo "🔄 Спроба $attempt з 5 отримання розміру..."
                
                # Отримуємо інформацію про файл з MinIO
                stat_output=$(mc stat "minio/$bucket_name/${bucket_name}.xml" 2>&1)
                
                if [ $? -eq 0 ]; then # Якщо команда mc stat успішна
                  # Витягуємо рядок з розміром. Шукаємо "Size :" незалежно від регістру та можливих пробілів на початку.
                  size_line=$(echo "$stat_output" | grep -i '^ *Size :')
                  
                  if [ -n "$size_line" ]; then
                    # Витягуємо чисельне значення розміру. Враховуємо числа з комами та пробілами.
                    # Приклад: "Size : 209 807 Bytes" або "Size : 2.1 MiB"
                    size_value_raw=$(echo "$size_line" | sed -E 's/^ *Size *: *([0-9.,]+)(.*)$/\1/' | tr -d ' ')
                    # Витягуємо одиницю виміру (літери).
                    size_unit_raw=$(echo "$size_line" | sed -E 's/^ *Size *: *[0-9.,]+[ ]*([a-zA-Z]+)$/\1/' | tr -d ' ')

                    # Якщо одиниця виміру не була знайдена або є порожньою, припускаємо "Bytes"
                    if [ -z "$size_unit_raw" ]; then
                        size_unit_raw="Bytes"
                    fi

                    # Конвертуємо витягнуте значення та одиницю у байти
                    remote_bytes=$(to_bytes "$size_value_raw" "$size_unit_raw")
                    
                    # Перевірка, чи конвертація пройшла успішно і результат > 0
                    if [ -n "$remote_bytes" ] && [ "$remote_bytes" -gt 0 ]; then
                      echo "📊 Розмір на MinIO: $remote_bytes байт ($size_value_raw $size_unit_raw)"
                      break # Успішно отримали розмір, виходимо з циклу спроб
                    else
                      echo "⚠️ Не вдалося конвертувати розмір або отримано нульове значення для '$size_value_raw' '$size_unit_raw'."
                    fi
                  else
                    echo "⚠️ Не вдалося знайти рядок 'Size :' у виводі 'mc stat'."
                  fi
                else
                  echo "❌ Помилка при виконанні 'mc stat' для 'minio/$bucket_name/${bucket_name}.xml': $stat_output"
                fi
                
                # Якщо не вдалося отримати розмір, чекаємо перед наступною спробою
                if [ -z "$remote_bytes" ] || [ "$remote_bytes" -eq 0 ]; then
                  echo "⏳ Очікування 3 секунди перед наступною спробою..."
                  sleep 3
                fi
              done # Кінець циклу спроб
              
              # Перевірка, чи після всіх спроб вдалося отримати розмір
              if [ -z "$remote_bytes" ] || [ "$remote_bytes" = "0" ]; then
                echo "❌ Не вдалося отримати дійсний розмір файлу '$filename' з MinIO після 5 спроб."
                exit 1
              fi
              
              # Порівняння локального та віддаленого розмірів з допуском
              # Допустима різниця у 1% (використовуємо bc для точних обчислень)
              threshold=$(echo "scale=0; $local_size / 100" | bc)
              diff=$(echo "$local_size - $remote_bytes" | bc)
              abs_diff=$(echo "$diff < 0 ? -$diff : $diff" | bc) # Розрахунок модуля різниці
              
              if [ "$abs_diff" -le "$threshold" ]; then
                echo "✅ Розміри файлу '$filename' співпадають (допуск ±$threshold байт)."
                echo "📊 Локальний: $local_size байт"
                echo "📊 Віддалений: $remote_bytes байт"
              else
                echo "⚠️ Розміри файлу '$filename' не співпадають!"
                echo "📊 Локальний: $local_size байт"
                echo "📊 Віддалений: $remote_bytes байт"
                echo "🔍 Різниця: $diff байт (допуск ±$threshold байт)."
                exit 1
              fi
            fi # Кінець перевірки існування файлу
          done # Кінець циклу файлів
          
          echo "🎉 Всі файли успішно завантажені та перевірені!"

      # Крок 13: Додаткова перевірка списку файлів
      - name: List all uploaded files
        run: |
          echo "📋 Список всіх завантажених файлів у MinIO:"
          # Перебираємо всі локальні згенеровані XML файли, щоб знати, які бакети мають існувати
          for xml_file in xml-outputs/*.xml; do
            if [ -f "$xml_file" ]; then
              bucket_name=$(basename "$xml_file" .xml)
              echo "🗂️ Бакет: $bucket_name"
              # Виводимо вміст бакету. Додаємо || true, щоб продовжити виконання, якщо бакет не існує або порожній.
              mc ls "minio/$bucket_name/" || echo "  (Бакет порожній або не існує)"
              echo "---"
            fi
          done