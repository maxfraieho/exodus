name: Generate Repository XML and Upload to MinIO

on:
  push:
    branches: [main]
    paths:
      - "src/site/notes/**"
      - "repomix.config.json"  # Додано для перезапуску при зміні конфігурації
  pull_request:
    branches: [main]
    paths:
      - "src/site/notes/**"
      - "repomix.config.json"

jobs:
  generate-and-upload:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip curl

      # Генерація основного XML файлу
      - name: Pack repository with Repomix
        uses: yamadashy/repomix/.github/actions/repomix@main
        with:
          output: repository.xml
          additional-args: --config repomix.config.json

      # Генерація окремих XML файлів для кожної папки (опціонально)
      - name: Generate separate XML files for each folder
        run: |
          mkdir -p xml-outputs
          
          # Копіюємо основний файл
          cp repository.xml xml-outputs/repository.xml
          
          # Перевіряємо чи існує src/site/notes/
          if [ -d "src/site/notes/" ]; then
            # Створюємо скрипт транслітерації
            cat > transliterate.sh << 'EOF'
            #!/bin/bash
            transliterate() {
                local input="$1"
                input=$(echo "$input" | sed 's/а/a/g; s/б/b/g; s/в/v/g; s/г/h/g; s/ґ/g/g; s/д/d/g; s/е/e/g; s/є/ye/g; s/ж/zh/g; s/з/z/g; s/и/y/g; s/і/i/g; s/ї/yi/g; s/й/y/g; s/к/k/g; s/л/l/g; s/м/m/g; s/н/n/g; s/о/o/g; s/п/p/g; s/р/r/g; s/с/s/g; s/т/t/g; s/у/u/g; s/ф/f/g; s/х/kh/g; s/ц/ts/g; s/ч/ch/g; s/ш/sh/g; s/щ/shch/g; s/ь//g; s/ю/yu/g; s/я/ya/g')
                input=$(echo "$input" | sed 's/А/A/g; s/Б/B/g; s/В/V/g; s/Г/H/g; s/Ґ/G/g; s/Д/D/g; s/Е/E/g; s/Є/Ye/g; s/Ж/Zh/g; s/З/Z/g; s/И/Y/g; s/І/I/g; s/Ї/Yi/g; s/Й/Y/g; s/К/K/g; s/Л/L/g; s/М/M/g; s/Н/N/g; s/О/O/g; s/П/P/g; s/Р/R/g; s/С/S/g; s/Т/T/g; s/У/U/g; s/Ф/F/g; s/Х/Kh/g; s/Ц/Ts/g; s/Ч/Ch/g; s/Ш/Sh/g; s/Щ/Shch/g; s/Ь//g; s/Ю/Yu/g; s/Я/Ya/g')
                input=$(echo "$input" | sed 's/[[:space:]]\+/-/g' | tr '[:upper:]' '[:lower:]')
                input=$(echo "$input" | sed 's/[^a-z0-9-]//g' | sed 's/--*/-/g; s/^-//; s/-$//')
                if [ ${#input} -lt 3 ]; then 
                    input="${input}bucket"
                fi
                if [ ${#input} -gt 63 ]; then 
                    input="${input:0:63}"
                fi
                echo "$input"
            }
            transliterate "$1"
            EOF
            chmod +x transliterate.sh
            
            # Генеруємо XML для кожної папки
            for folder in src/site/notes/*/; do
              if [ -d "$folder" ]; then
                folder_name=$(basename "$folder")
                bucket_name=$(./transliterate.sh "$folder_name")
                
                echo "🔄 Генерація XML: $folder_name -> $bucket_name"
                
                # Використовуємо GitHub Action для генерації
                npx repomix --config repomix.config.json --include "$folder/**" --output "xml-outputs/${bucket_name}.xml"
                
                if [ -s "xml-outputs/${bucket_name}.xml" ]; then
                  echo "✅ Згенеровано: ${bucket_name}.xml"
                else
                  echo "⚠️ Не вдалося згенерувати: ${bucket_name}.xml"
                fi
              fi
            done
            
            # Генеруємо common.xml для кореневих файлів
            npx repomix --config repomix.config.json --ignore "src/site/notes/**" --output "xml-outputs/common.xml"
            
            if [ -s "xml-outputs/common.xml" ]; then
              echo "✅ Згенеровано: common.xml"
            else
              echo "⚠️ Не вдалося згенерувати: common.xml"
            fi
          fi

      - name: Verify generated XML files
        run: |
          echo "📋 Перевірка згенерованих XML файлів:"
          
          if [ ! -s repository.xml ]; then
            echo "❌ Помилка: repository.xml порожній або не створений"
            exit 1
          fi
          
          echo "✅ repository.xml розміром $(stat -c%s repository.xml) байт"
          
          # Перевіряємо інші XML файли
          if [ -d "xml-outputs/" ]; then
            for xml_file in xml-outputs/*.xml; do
              if [ -f "$xml_file" ]; then
                size=$(stat -c%s "$xml_file")
                if [ "$size" -gt 0 ]; then
                  echo "✅ $(basename "$xml_file") розміром $size байт"
                else
                  echo "⚠️ $(basename "$xml_file") порожній"
                fi
              fi
            done
          fi

      - name: Upload XML files as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xml-outputs
          path: |
            repository.xml
            xml-outputs/
          retention-days: 7

      - name: Install AWS CLI v2
        run: |
          curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
          unzip -q awscliv2.zip
          sudo ./aws/install --update
          aws --version

      - name: Configure AWS CLI for MinIO via Cloudflare
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MINIO_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MINIO_SECRET_KEY }}
        run: |
          aws configure set aws_access_key_id "$AWS_ACCESS_KEY_ID"
          aws configure set aws_secret_access_key "$AWS_SECRET_ACCESS_KEY"
          aws configure set default.region us-east-1
          aws configure set default.s3.addressing_style path
          aws configure set default.s3.signature_version s3v4
          
          # Тестуємо з'єднання
          echo "🔐 Тестування з'єднання з MinIO..."
          if aws --endpoint-url https://apiminio.exodus.pp.ua s3 ls; then
            echo "✅ З'єднання з MinIO успішне"
          else
            echo "❌ Помилка з'єднання з MinIO"
            exit 1
          fi

      - name: Upload Repository XML to MinIO
        env:
          AWS_S3_DISABLE_MULTIPART: "1"
        run: |
          echo "🚀 Завантаження repository.xml до MinIO..."
          
          # Завантажуємо основний файл
          aws --endpoint-url https://apiminio.exodus.pp.ua \
            s3api put-object \
            --bucket exodusppua \
            --key "repository.xml" \
            --body "repository.xml" \
            --content-type application/xml \
            --cli-read-timeout 360 \
            --cli-connect-timeout 360
          
          if [[ $? -ne 0 ]]; then
            echo "❌ Помилка під час завантаження repository.xml"
            exit 1
          fi
          
          echo "✅ Успішно завантажено: repository.xml"

      - name: Upload individual XML files to MinIO
        env:
          AWS_S3_DISABLE_MULTIPART: "1"
        run: |
          if [ -d "xml-outputs/" ]; then
            echo "🚀 Завантаження окремих XML файлів..."
            
            # Створюємо скрипт для створення бакетів
            cat > create_bucket.sh << 'EOF'
            #!/bin/bash
            bucket_name="$1"
            if aws --endpoint-url https://apiminio.exodus.pp.ua s3api head-bucket --bucket "$bucket_name" 2>/dev/null; then
              echo "ℹ️ Бакет '$bucket_name' вже існує"
            else
              echo "🔨 Створення бакету '$bucket_name'"
              aws --endpoint-url https://apiminio.exodus.pp.ua s3api create-bucket --bucket "$bucket_name" --region us-east-1
            fi
            EOF
            chmod +x create_bucket.sh
            
            for xml_file in xml-outputs/*.xml; do
              if [ -f "$xml_file" ] && [ -s "$xml_file" ]; then
                bucket_name=$(basename "$xml_file" .xml)
                
                echo "📦 Завантаження: $xml_file -> бакет: $bucket_name"
                
                # Створюємо бакет
                ./create_bucket.sh "$bucket_name"
                
                # Завантажуємо файл
                aws --endpoint-url https://apiminio.exodus.pp.ua \
                  s3api put-object \
                  --bucket "$bucket_name" \
                  --key "${bucket_name}.xml" \
                  --body "$xml_file" \
                  --content-type application/xml \
                  --cli-read-timeout 360 \
                  --cli-connect-timeout 360
                
                if [[ $? -eq 0 ]]; then
                  echo "✅ Завантажено: ${bucket_name}.xml"
                else
                  echo "⚠️ Помилка завантаження: ${bucket_name}.xml"
                fi
              fi
            done
          fi

      - name: Verify uploaded files
        run: |
          echo "🔍 Перевірка завантажених файлів..."
          
          # Перевіряємо основний файл
          if aws --endpoint-url https://apiminio.exodus.pp.ua s3api head-object --bucket exodusppua --key "repository.xml" > /dev/null 2>&1; then
            echo "✅ repository.xml знайдено в бакеті exodusppua"
          else
            echo "❌ repository.xml не знайдено в бакеті exodusppua"
          fi
          
          # Перевіряємо окремі файли
          if [ -d "xml-outputs/" ]; then
            for xml_file in xml-outputs/*.xml; do
              if [ -f "$xml_file" ] && [ -s "$xml_file" ]; then
                bucket_name=$(basename "$xml_file" .xml)
                
                if aws --endpoint-url https://apiminio.exodus.pp.ua s3api head-object --bucket "$bucket_name" --key "${bucket_name}.xml" > /dev/null 2>&1; then
                  echo "✅ ${bucket_name}.xml знайдено в бакеті $bucket_name"
                else
                  echo "❌ ${bucket_name}.xml не знайдено в бакеті $bucket_name"
                fi
              fi
            done
          fi
